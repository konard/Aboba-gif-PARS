# parser.py — описание

Скрипт читает STEP-файл, определяет тип каждой поверхности (плоскость, цилиндр, конус и т.д.) и выгружает точные параметры в JSON для дальнейшего использования.

---

## Как запустить
```yaml
name: cad_ml
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pythonocc-core=7.9.3
  - pyvista      # Визуализация
  - trimesh      # Работа с мешами
  - pandas       # Таблицы данных
  - tqdm         # Прогресс-бары
  - pip
  - pip:
      - ezdxf    # Экспорт в DXF (через pip, так как в conda может не быть свежей версии)
```
```bash
# 1. Активировать окружение 
conda activate cad_ml

# 2. Запустить скрипт
python parser.py
```

**Что произойдёт:**
1. Скрипт найдёт файл  в текущей папке
2. Проанализирует все грани
3. Выведет в консоль краткую статистику:
   ```
   Загрузка STEP...
   Анализ геометрии граней...
   Обработано граней: 8
   ТИПЫ ПОВЕРХНОСТИ: {'PLANE': 6, 'CYLINDER': 2}
   Данные сохранены в drafting_data.json
   ```
4. Сохранит результат в `drafting_data.json`

---

## Что на выходе: `drafting_data.json`

Файл содержит список всех граней. Пример одной записи:

```json
{
  "face_id": 2,
  "type": "CYLINDER",
  "params": {
    "radius": 12.5,
    "axis": {
      "location": [0.0, 0.0, 0.0],
      "axis_direction": [0.0, 0.0, 1.0]
    }
  },
  "area": 1570.8
}
```

### Простая расшифровка полей:

| Поле | Что значит | Зачем нужно |
|------|-----------|-------------|
| `face_id` | Номер грани (0, 1, 2...) | Чтобы ссылаться на конкретную грань в других скриптах |
| `type` | Тип поверхности | Понимать, как отображать на чертеже: окружность (цилиндр), прямоугольник (плоскость) и т.д. |
| `params.radius` | Радиус (для цилиндров, сфер, конусов) | Для простановки размеров `Ø25`, `R10` |
| `params.axis` | Ось + точка привязки | Чтобы провести осевую линию в нужном месте |
| `params.normal` | Вектор нормали (для плоскостей) | Чтобы понять, куда "смотрит" грань — для выбора вида на чертеже |
| `area` | Площадь грани в мм² | Чтобы отфильтровать мелкие фаски или технологические элементы |

---

## Типы поверхностей, которые распознаёт скрипт 

| Тип | Что это | Пример на чертеже |
|-----|---------|------------------|
| `PLANE` | Плоскость | Прямоугольник, вид спереди/сбоку |
| `CYLINDER` | Цилиндр | Отверстие `Ø12`, вал, посадочная поверхность |
| `CONE` | Конус | Коническая посадка, фаска под углом |
| `SPHERE` | Сфера | Сферическая головка, шарнир |
| `TORUS` | Тор (бублик) | Галтель, канавка под уплотнение |
| `FREEFORM_SURFACE` | Свободная форма (NURBS) | Сложная криволинейная поверхность — требует ручной доработки |
| `COMPLEX_SURFACE` | Не распознано | Стоит проверить модель на ошибки импорта |

---

## Что нужно для работы

| Зависимость | Как проверить |
|-------------|--------------|
| Конда-окружение `cad_ml` | `conda env list` — должно быть в списке |
| `pythonocc-core` | `python -c "import OCC; print(OCC.__version__)"` |
| Файл `.step` | Должен лежать в папке со скриптом или указать путь в коде |

> **Важно**: Скрипт работает только с **аналитическими поверхностями**. Если модель импортирована из полигонального формата (STL, OBJ) или содержит только меши — все грани будут определены как `COMPLEX_SURFACE`.

---

## Если что-то пошло не так

| Симптом | Возможная причина | Что делать |
|---------|------------------|------------|
| `ImportError: No module named 'OCC'` | Не активировано окружение | `conda activate cad_ml` |
| `File not found: test1.step` | Нет входного файла | Положите STEP-файл в папку или поправьте путь в коде |
| Все грани `COMPLEX_SURFACE` | Модель содержит только меши | Проверить исходный CAD: экспортировать STEP с сохранением аналитики |
| Скрипт «висит» на больших файлах | Много граней (>10 000) | Это нормально; можно добавить фильтрацию по `area` для ускорения |

---
